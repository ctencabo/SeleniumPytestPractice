""" This file is a collection of all the page elements and the action methods 
    of the landing page.

    The elements are identified using;
        -- xpath (Relative xpath recommended)
        -- id
        -- css-selectors and many others

    The action methods perform the actions on the page like;
        -- clicking
        -- verifying if elements are enabled and displayed
        -- Sending keys
        -- Selecting elements
"""
class LandingPageElements:

    # Labels/Texts
    nav_bar_call_us_now_text = ".//span[@class='shop-phone']"
    nav_bar_contact_us_text = ".//a[@title='Contact Us']"
    nav_bar_sign_in_text = ".//a[@class='login']"

    # Buttons

    # Search Bars

    # Dropdowns

    # Images
